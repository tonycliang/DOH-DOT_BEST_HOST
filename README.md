## 📌 功能简介


- **入口：main.py
- **自动读取域名列表**：从 `Domains2HOST.ini` 文件加载待处理的域名。
- **智能 DNS 解析**：使用多 DNS 服务器查询，通过投票机制筛选可信 IP（防污染）。
- **Ping 网络测试**：对每个 IP 进行连通性测试，记录延迟和丢包率。
- **智能排序与筛选**：按丢包率和延迟排序，保留最优 IP。
- **生成标准 HOSTS 文件**：输出格式规范、带注释说明的 `HOST.txt`，可直接替换系统 hosts。
- **日志与调试支持**：支持日志记录和异常定位，便于排查问题。
